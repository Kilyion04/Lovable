import { Layout } from "@/components/layout/Layout";

const UserSettings = () => {
  return (
    <Layout>
      <section className="py-20">
        <h1 className="text-3xl font-bold mb-6">Paramètres Utilisateur</h1>
        <p>Personnalisez vos préférences ici.</p>
        {/* Add settings form or options */}
      </section>
    </Layout>
  );
};

export default UserSettings;